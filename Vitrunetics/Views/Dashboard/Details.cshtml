@using Vitrunetics.BootstrapSupport
@model Vitrunetics.Patient
@{int PatientID;}
<fieldset>
    <legend>@Model.GetLabel() <small>Details</small></legend>
    <dl> <!-- use this class on the dl if you want horizontal styling http://twitter.github.com/bootstrap/base-css.html#typography  class="dl-horizontal"-->     
        @foreach(var property in Model.VisibleProperties())
        {
            if (property.Name.Equals("PatientID") || property.Name.Equals("TherapistID") || property.Name.Equals("UserId") || property.Name.Equals("CreatedDate") || property.Name.Equals("UserProfile") || property.Name.Equals("PatientRegimen")) { }
            else 
            { 
                <dt>
                    @property.GetLabel().ToSeparatedWords()
                </dt>
                <dd>
                    @Html.Display(property.Name) 
                </dd>
            }
            if (property.Name.Equals("PatientID"))
            {
                //property.
            }
            
        }
    </dl>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.PatientID})
    @Html.ActionLink("Back to List", "Dashboard")
</p>
