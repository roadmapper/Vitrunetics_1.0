@model Vitrunetics.Regimen

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Regimen</legend>

        @Html.HiddenFor(model => model.RegimenID)

        <div class="editor-label">
            @Html.LabelFor(model => model.RegimenName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.RegimenName)
            @Html.ValidationMessageFor(model => model.RegimenName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
