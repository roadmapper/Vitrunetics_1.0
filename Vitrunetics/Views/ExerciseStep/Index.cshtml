@model IEnumerable<Vitrunetics.ExerciseStep>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DelayTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Instructions)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExerciseSequence.ExerciseSequenceID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DelayTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Instructions)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExerciseSequence.ExerciseSequenceID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExerciseStepID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExerciseStepID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExerciseStepID })
        </td>
    </tr>
}

</table>
